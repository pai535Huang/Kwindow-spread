{
  "name": "kwindow-spread",
  "version": "1.1.0",
  "description": "KWin script package for Kwindow-spread.",
  "type": "module",
  "private": true,
  "scripts": {
    "test": "node --test && npm run check:repository",
    "check:repository": "git ls-files -z | node scripts/check-repository-policy.mjs"
  },
  "license": "MIT"
}
